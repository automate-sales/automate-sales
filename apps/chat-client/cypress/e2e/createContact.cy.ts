describe('test that new contacts get created when they send a message', () => {
    it('should should display new message at the top of the sidebar', () => {
        // new message should have an exclamation mark as it hasnt been seen/ answered
        // click on the contact that sent you the message
        // should take you to contact page and get the Id from the URL
    })
})